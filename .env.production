# 프로덕션 빌드(npm run build)에서만 읽힌다. 개발서버(npm start)는 .env.development / 기본값을 쓴다.
#
# 배포 도메인이 정해지면 아래 값을 채운다.
# Cloudflare Pages 를 쓴다면 이 파일 대신 대시보드의 "빌드 환경변수"에 넣는 편이 낫다
# (도메인이 바뀌어도 코드를 건드리지 않는다).
#
# 비워두면 src/config/api.js 의 기본값 http://localhost:8081 로 빌드되므로
# 배포 전에 반드시 채울 것.
REACT_APP_API_URL=
