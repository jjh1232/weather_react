# 개발서버(npm start)에서만 읽힌다. 빌드(npm run build)는 .env.production 을 쓴다.
#
# 예전엔 package.json 의 "proxy" 가 이 역할을 대신했는데, proxy 는 CRA 개발서버
# 전용이라 빌드 결과물에는 존재하지 않는다. 그래서 배포하면 상대경로 호출이
# 전부 프론트 도메인으로 나가 404 가 났다. 이제 주소는 src/config/api.js 한 곳에서만 읽는다.
REACT_APP_API_URL=http://localhost:8081
